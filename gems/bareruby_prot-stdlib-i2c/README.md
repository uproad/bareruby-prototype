# The I2C class

One class the language offers, packaged: what a program may say on a bus, and what each of those calls lowers to.

Nothing in the compiler names it. It is found because it is installed, at the path every
standard class is looked for under. Naming `I2C` in a program is what brings this gem
into the picture, and installing it is what brings the compiler with it.

# The PWM class

One class the language offers, packaged: what a program may ask of a timer channel, and what each of those calls lowers to.

Nothing in the compiler names it. It is found because it is installed, at the path every
standard class is looked for under. Naming `PWM` in a program is what brings this gem
into the picture, and installing it is what brings the compiler with it.

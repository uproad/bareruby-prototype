# The UART class

One class the language offers, packaged: what a program may send and receive on a port, and what each of those calls lowers to.

Nothing in the compiler names it. It is found because it is installed, at the path every
standard class is looked for under. Naming `UART` in a program is what brings this gem
into the picture, and installing it is what brings the compiler with it.

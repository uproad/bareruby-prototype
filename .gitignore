# Generated by the first stage, rewritten in full on every run
/dump/
/build/

# The SDKs and toolchains each API builds with, downloaded or built in place, one
# directory per API and each named for its version. Here because the build reaches for
# them; out of git because they are other people's releases.
/.tools/

# Which boards are on this desk is a fact about a desk, not about the project.
# Copy target.yml.sample to start one.
/target.yml
